namespace EconomiMM.Enum
{
    public enum JointShapes
    {
        None = -1,
        Circle,
        Rectangle
    }
    public enum JointType
    {
        None = -1,
        Flange,
        Tape
    }
}
