@model EconomiMM.ViewModels.CalculatorViewModel
@{
}
<div class="joint-material border border-1 scroll-main-window">

    <button type="button" class="btn btn-light add-material-btn" data-bs-toggle="modal" data-bs-target="#joint-material-Modal">
        <img class="filter-icon" src="~/img/filter.png" />
    </button>



</div>
<div class="modal" id="joint-material-Modal" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Додати матеріал</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Назад"></button>
            </div>

            <div class="modal-body">
                <label class="control-label">Виробник</label>
                <div class="joint-material-modal border border-1 scroll">
                    @foreach (var materialType in Model.jointMaterialTypes)
                    {
                        <a id="@materialType.Id" class="toggle-btn link-dark">@materialType.Name</a>
                        <div id="materialType-@materialType.Id">
                            @foreach (var material in Model.expansionJointMaterials)
                            {
                                @if (material.Type == materialType)
                                {
                                    <div>
                                        <input class="form-check-input" type="checkbox" value="" id="@material.normilized_name">
                                        <label class="form-check-label" for="@material.normilized_name">
                                            @material.Name  -----   @material.Price
                                        </label>
                                        <br>
                                    </div>
                                }

                            }
                        </div>
                    }


                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Назад</button>
                <button id="add-joint-material-button" type="submit" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#joint-material-Modal">Підтвердити</button>
            </div>

        </div>
    </div>
</div>